네, 그럼요! 처음에 주셨던 질문 리스트에 우리가 함께 결정한 답변을 모두 채워서, 완전한 기술 명세서 형태로 만들어 드릴게요.

---
## **'나만의 카나나' 데모 - 상세 기술 명세서 (Q&A)**

### **사양/흐름 (Specification/Flow)**

**Q. “카나나” 생성 플로우의 화면 전환 구조(라우팅 경로, 페이지 vs 모달)와 빈 상태(목록 비어있을 때) 정의가 있나요?**
* **A.** 화면 전환은 별도의 **페이지(`/create`)로 이동**하는 구조를 따릅니다. 목록이 비어있을 경우, "만든 카나나가 없어요. 첫 번째 카나나를 만들어보세요!"와 같은 안내 문구와 만들기 버튼을 포함한 **빈 상태(Empty State) UI**를 명확히 표시합니다.

**Q. 카나나 이름/규칙 입력의 유효성 기준(최대/최소 길이, 금지 문자, 필수 여부, 에러 메시지)은?**
* **A.**
    * **이름:** **필수 입력**, 최소 2자/최대 20자, 특수문자/이모지 금지.
    * **역할 및 규칙:** **필수 입력**, 최소 10자/최대 500자.
    * **에러 메시지:** 각 위반 규칙에 맞춰 "이름을 입력해주세요.", "이름은 2자 이상 입력해주세요." 등 명확한 안내 메시지를 표시합니다.

**Q. 카테고리(업무/학습/…) 정의와 초기 제공 템플릿 3~5개의 정확한 내용/메타정보는?**
* **A.**
    * **카테고리:** `업무 헬퍼`, `학습 튜터`, `재미 & 심심풀이` 3가지로 정의합니다.
    * **초기 템플릿:**
        1.  **(업무) 이메일 마스터:** 비즈니스 이메일 초안을 작성해주는 카나나
        2.  **(학습) 친절한 영어 튜터:** 자연스러운 영어 번역과 문법을 설명해주는 카나나
        3.  **(재미) 아무말 대잔치:** 엉뚱하고 유머러스한 답변을 하는 카나나

---
### **API/Gemini**

**Q. Gemini 1.5 Flash 호출의 엔드포인트/인증 방식/요청·응답 스키마 예시를 제공할 수 있나요?**
* **A.** 네, 아래와 같습니다.
    * **Endpoint:** `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent`
    * **인증:** URL 파라미터에 API 키를 포함하여 인증합니다. (`?key=YOUR_API_KEY`)
    * **요청 스키마:** `{ "system_instruction": { "parts": { "text": "..." } }, "contents": [ { "role": "user", "parts": { "text": "..." } } ] }`
    * **응답 스키마:** `data.candidates[0].content.parts[0].text` 경로로 답변 텍스트를 추출합니다.

**Q. 네트워크 사용이 어려운 환경 대비 목업 서버/레코드(예: MSW, JSON fixtures)로 진행해도 되나요?**
* **A.** 아니요, 데모의 현장감을 위해 **실제 Gemini API를 직접 호출**합니다.

**Q. 요청 실패/타임아웃/레이트리밋 시 재시도·Fallback 정책은?**
* **A.** 재시도 로직은 구현하지 않습니다. 모든 API 오류 발생 시, **"죄송합니다, 지금은 답변을 드릴 수 없어요. 잠시 후 다시 시도해주세요."** 라는 공통 Fallback 메시지를 표시합니다.

---
### **데이터/상태 (Data/State)**

**Q. 모든 데이터 로컬 저장 기준에서 localStorage 키/스키마를 확정해 주세요.**
* **A.**
    * **Key:** `kakaotalk:kanana_list`
    * **Schema:** `[ { "id": "...", "name": "...", "instructions": "...", "createdAt": "..." } ]`

**Q. ID 생성 규칙(UUID/타임스탬프)와 정렬 기준(최신순/이름순)은?**
* **A.** ID는 **UUID**로 생성하며, 목록 정렬은 **최신순**으로 합니다.

---
### **기술/도구 (Technology/Tools)**

**Q. Next.js 버전(App Router 사용 여부)과 패키지 매니저(npm/pnpm/yarn), Node.js LTS 버전은?**
* **A.**
    * **Node.js:** 최신 LTS 버전
    * **Next.js:** 최신 버전 (App Router 사용)
    * **패키지 매니저:** npm

**Q. 스타일 도구는 Tailwind와 styled-components 중 하나로 고정할까요? 혼용 금지인지?**
* **A.** **styled-components**만 사용하며, 다른 도구와 혼용하지 않습니다.

**Q. Axios 전역 인터셉터(에러/토큰) 사용 여부와 로깅 방침은?**
* **A.** 전역 **에러 인터셉터**를 사용하여 오류를 일괄 처리합니다. 로깅은 개발 환경에서만 `console.log`를 사용합니다.

---
### **UI/디자인 (UI/Design)**

**Q. 디자인 토큰을 CSS 변수로 정의해도 되나요? 변수명 가이드(예: --color-primary 등)를 제공해 주세요.**
* **A.** 네, CSS 변수로 정의합니다. `--color-primary`, `--font-size-body` 와 같이 직관적인 변수명을 사용합니다.

**Q. 반응형 기준(모바일/태블릿/데스크톱 브레이크포인트)과 최소 지원 해상도는?**
* **A.** **모바일 화면에만 집중**하며, 최소 지원 너비는 **360px**로 설정합니다.

**Q. 접근성 기준(명도 대비, 포커스 스타일, 키보드 내비게이션) 목표가 있나요?**
* **A.** **WCAG AA 기준 색상 대비**와 **시맨틱 HTML 사용**을 목표로 합니다.

---
### **테스트/품질 (Test/Quality)**

**Q. 테스트 스택(Jest + RTL, Playwright 등)과 커버리지 목표(예: 변경 코드 80% 이상)를 확정해 주세요.**
* **A.** 테스트 코드 작성은 데모 **범위에서 제외**합니다. (단, 실제 프로젝트라면 Jest+RTL 사용 계획)

**Q. 린트/포맷터(ESLint/Prettier) 규칙과 CI에서의 필수 체크 목록은?**
* **A.** **ESLint와 Prettier의 표준 규칙**을 따릅니다. CI 구축은 데모 범위에서 제외합니다.

---
### **기타 (Misc)**

**Q. 문서의 한글 인코딩이 일부 깨져 보입니다. 원문 또는 올바른 인코딩 버전으로 업데이트 가능할까요?**
* **A.** 모든 파일은 **UTF-8**로 저장하는 것을 원칙으로 합니다.

**Q. 추후 국제화(i18n) 계획 여부와 기본 Locale은?**
* **A.** 데모는 **한국어**만 지원하며, 국제화는 고려하지 않습니다.